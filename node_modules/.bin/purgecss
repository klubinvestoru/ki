#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@fullhuman/postcss-purgecss/node_modules/purgecss/bin/purgecss" "$@"
  ret=$?
else 
  node  "$basedir/../@fullhuman/postcss-purgecss/node_modules/purgecss/bin/purgecss" "$@"
  ret=$?
fi
exit $ret
